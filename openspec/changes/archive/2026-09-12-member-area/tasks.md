# Tasks

- [x] `AuthUser` 型別 + `AuthContext` 兩處 user 建構點補上 `id`
- [x] `useCourseActions` 擴充 `cancelBooking`
- [x] `DashboardView` 接上真實課表 + 取消報名
- [x] `ProfileView` 接上改暱稱／改密碼
- [x] `OrdersView` 接上真實購買紀錄
- [x] `BecomeCoachView` 接上真實升級表單，成功後登出並導向 `/login`（不偷改當前 session 的 role）
- [x] `npm run build` 通過
