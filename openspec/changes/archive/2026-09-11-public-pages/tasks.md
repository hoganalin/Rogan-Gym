# Tasks

- [x] `lib/errors.ts`、`lib/formatDateTime.ts` 共用工具
- [x] `hooks/useCourseActions.ts`（報名）、`hooks/usePackageActions.ts`（購買），SweetAlert 確認/成功/失敗流程
- [x] `HomeView` 接上近期課程／熱門教練
- [x] `CoachesView` 接上真實分頁教練列表
- [x] `CoachDetail` 接上教練檔案／課程／報名
- [x] `FitnessPlans` 接上方案／購買
- [x] 修正 `useCourseActions` 在 auth 狀態還在還原時（AuthContext 掛載中）誤判成未登入的 race condition
- [x] `npm run build` 通過
